import React from 'react'; 

// Not currently used but here for animations etc.

const Loader = () => {
    return (
        <div>
            Loading...
        </div>
    );
}

export default Loader; 